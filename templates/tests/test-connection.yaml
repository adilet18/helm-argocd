apiVersion: v1
kind: Pod
metadata:
  name: test-connection
  labels:
    app: test-connection
spec:
  containers:
  - name: curl-container
    image: curlimages/curl:latest
    command: ["sleep", "3600"]  # Keep the pod running for testing
  restartPolicy: Never
