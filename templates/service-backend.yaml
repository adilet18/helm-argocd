apiVersion: v1
kind: Service
metadata:
  name: myapp-backend
  namespace: argocd
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.backend.service.port }}
  selector:
    app: myapp-backend
