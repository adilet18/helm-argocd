apiVersion: v1
kind: Service
metadata:
  name: myapp-frontend
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.frontend.port }}
  selector:
    app: myapp-frontend
